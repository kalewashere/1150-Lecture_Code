languages = ['Python', 'Java', 'JavaScript', 'C#', 'Swift'] # if .remove() is used on an element in a list that has
# it listed twice, the program automatically removes the first instance of duplicate element in list also
# case-sensitive, must tell program what to remove letter by letter, if something is different (s vs. S) it will not
# return anything (broken) bc the program doesnt recognize the the spelling, therefore it is not there
for language in languages:  # singular + list name
    print(language)  # will print on each line
